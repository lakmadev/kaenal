import { Placeholder } from "@/features/shared/Placeholder";

export default function Me() {
  return (
    <Placeholder
      title="Settings"
      overline="Profile & preferences"
      icon="user"
      body="Profile, security (MFA, biometric, sessions), offline & storage, notifications and appearance."
      phase="M11"
    />
  );
}
