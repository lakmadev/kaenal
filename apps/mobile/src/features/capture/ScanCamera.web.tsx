// Web stub for the native live scanner — never rendered (scan.tsx shows the
// manual-entry fallback on web), but present so the platform-extension import
// resolves and `expo-camera` stays out of the web bundle.
export function ScanCamera(_: { onScan: (value: string) => void }): null {
  return null;
}
