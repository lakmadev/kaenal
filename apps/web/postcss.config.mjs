/** Tailwind CSS v4 is a PostCSS plugin — no `tailwind.config` file needed;
 *  the theme lives in `src/styles/globals.css` via `@theme` (04 §2). */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
